name: DB Migration Reminder

on:
  workflow_dispatch:

jobs:
  show:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print migration file path
        run: |
          echo "Apply this SQL in Neon SQL editor:"
          echo "server/sql/neon_bootstrap.sql"
