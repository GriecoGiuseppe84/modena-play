name: DB Migration Reminder

on:
  workflow_dispatch:

jobs:
  show:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print migration file path
        run: |
          echo "Apply this SQL in Supabase SQL editor:"
          echo "server/src/database/migrations/001_init_schema.sql"
